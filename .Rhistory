quit()
roxygen2::roxygenise()
roxygen2::roxygenise()
quit()
roxygen2::roxygenise()
quit()
roxygen2::roxygenise()
quit()
quit()
roxygen2::roxygenise()
quit()
roxygen2::roxygenise()
quit()
roxygen2::roxygenise()
quit()
covr::package_coverage(type = "all")
install.packages("covr"=)
install.packages("covr")
covr::package_coverage(type = "all")
data(airway, package="airway")
se <- airway
library(XAItest)
results <- XAI.test(se[1:100,], y = "dex", verbose = TRUE, simData = TRUE)
se[1:100, ]
head(se[1:100, ])
head(se[1:100, ])
is(dse[100:], "SummarizedExperiment")
is(dse[1:100,], "SummarizedExperiment")
is(se[1:100,], "SummarizedExperiment")
data = se[1:100,]
data_matrix <- assay(data, "counts")
data_matrix <- t(data_matrix)
metadata <- as.data.frame(colData(data))
if ( is.factor(metadata[[y]]) ){metadata[[y]] <- as.character(metadata[[y]])}
y="dex"
if ( is.factor(metadata[[y]]) ){metadata[[y]] <- as.character(metadata[[y]])}
data <- as.data.frame(cbind(data_matrix, y = metadata[[y]]))
colnames(data)[ncol(data)] <- y
for (col in names(data)) {if (col != y && !is.numeric(data[[col]])) {data[[col]] <- as.numeric(as.character(data[[col]]))}}
is(data[[y]], "character")
modelType <- "classification"
data
results <- XAI.test(se[1:100,], y = "dex", verbose = TRUE, simData = TRUE)
covr::package_coverage(type = "all")
covr::package_coverage(type = "test")
covr::package_coverage(type = "all")
covr::package_coverage(type = "all")
covr::package_coverage(type = "all")
covr::package_coverage(type = "all")
covr::package_coverage(type = "all")
covr::package_coverage(type = "all")
covr::package_coverage(type = "all")
covr::package_coverage(type = "all")
covr::package_coverage(type = "all")
covr::package_coverage(type = "all")
covr::package_coverage(type = "all")
covr::package_coverage(type = "all")
quit()
